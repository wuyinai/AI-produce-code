spring:
  application:
    name: wu-ai-pdce
  profiles:
    active: local
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wu_ai_pdce_code
    username: root
    password: 15288638209abcd

server:
  port: 8234
  # 路径前缀，任何API加上这个路径才能访问到服务器端
  servlet:
    context-path: /api

springdoc:
  group-configs:
    - group: "default"
      packages-to-scan: com.wuyinai.wuaipdce.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn